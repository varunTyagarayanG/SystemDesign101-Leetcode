# Use an official lightweight image based on Alpine Linux
FROM alpine:latest

# Set the working directory inside the container
WORKDIR /app

# Command to print "Hello, World!"
CMD ["echo", "Hello, World!"]
